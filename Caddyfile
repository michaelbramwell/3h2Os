{
    # Global options
    email {$ACME_EMAIL}
}

auth.{$DOMAIN} {
    reverse_proxy keycloak:8080
}

{$DOMAIN} {
    # Frontend
    reverse_proxy frontend:80

    # Backend API (adjust path if needed, assuming /api prefix or similar)
    handle /api* {
        reverse_proxy backend:8000
    }
}
